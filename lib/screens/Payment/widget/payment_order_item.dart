import 'package:flutter/material.dart';

class PaymentOrderItem extends StatelessWidget {
  const PaymentOrderItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
