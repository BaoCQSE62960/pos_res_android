import 'package:flutter/material.dart';

class PaymentSubTotal extends StatelessWidget {
  const PaymentSubTotal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
