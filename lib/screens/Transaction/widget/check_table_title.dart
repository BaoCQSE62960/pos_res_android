import 'package:flutter/material.dart';

class CheckTableTitle extends StatelessWidget {
  const CheckTableTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
